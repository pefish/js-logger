{
  "name": "@pefish/js-logger",
  "version": "1.0.8",
  "types": "./types/index.d.ts",
  "main": "./lib/index.js",
  "scripts": {
    "test": "mocha --require ts-node/register src/*.test.ts",
    "t": "mocha --require ts-node/register",
    "build": "rm -rf types lib && tsc",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "log4js": "^6.9.1"
  },
  "devDependencies": {
    "@types/chai": "^5.0.1",
    "@types/mocha": "^10.0.9",
    "@types/node": "^22.9.0",
    "mocha": "^11.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
